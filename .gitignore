# Node-Abhängigkeiten (nur lokal für die Playwright-Tests installiert,
# nicht vendored — siehe CLAUDE.md «Rollen×Modul-Matrix-Test»)
node_modules/

# Datenbank-Auszüge aus dem ERP-Altsystem (Analyse-Material für die Migration).
# Enthalten echte Kunden-, Adress- und Betragsdaten und gehören NIE ins Repo —
# der Weg nach GEMA läuft über CSV/XLSX in den Import-Assistenten (pm_erp),
# nicht über die Dumps selbst.
erp_alt/
*.db
*.sql.gz
*.bak
*.mdb
*.accdb
*.sqlite
*.sqlite3
*.fdb
*.dbf
